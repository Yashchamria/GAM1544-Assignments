#pragma once

const float TANK_MAX_SPEED = 450.0f;
const float TANK_ANGULAR_VELOCITY = 100.0f;

const float TANK_ACCELERATION = 180.0f;
const float TANK_DRAG = 0.05f;

const float SHELL_SPEED = 800.0f;

const float TURRENT_RADIUS = 145.0f - (25.0f * 145.0f) / 100.0f;


GameDev2D::Vector2 SCREEN_CENTRE = GameDev2D::Vector2(WINDOW_WIDTH / 2, WINDOW_HEIGHT / 2);